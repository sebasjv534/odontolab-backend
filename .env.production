# Ejemplo de variables de entorno para producción
# Estas se configuran en Render, NO subir este archivo con valores reales

# Database Configuration (PostgreSQL en Render)
DATABASE_URL=postgresql+asyncpg://username:password@hostname:port/database_name

# JWT Configuration (GENERAR UNA CLAVE SEGURA NUEVA)
SECRET_KEY=your-super-secure-secret-key-here-min-32-characters-long-for-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# API Configuration
DEBUG=true  # Mantener en true para acceder a /docs en Render
ENVIRONMENT=production

# CORS Origins (separados por comas)
CORS_ORIGINS=https://your-frontend-domain.com,https://your-app-domain.com